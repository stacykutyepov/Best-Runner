

// export const filterByDate = (workoutItems) => {
//     const filteredItems = workoutItems.slice()
//         .sort((a, b) => new Date(b.date) - new Date(a.date));
//         return [...filteredItems]
// }